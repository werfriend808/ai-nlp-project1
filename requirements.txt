# 실행에 필요한 외부 패키지 목록 (agent/, db/, tests/ 전체 import 기준으로 추출함,
# 2026-08-04 기준 WSL venv에서 검증된 버전).
#
# sentence-transformers가 torch/transformers를 함께 설치하는데, GPU(CUDA) 빌드가 잡히는지
# CPU 전용 빌드가 잡히는지는 플랫폼에 따라 pip가 알아서 고름 — 그래서 torch 자체는 버전을
# 못박지 않음. reranker.py/embedding_search.py에 있는 주석대로 일부 Windows CPU/torch
# 조합에서 세그폴트가 나는 게 알려져 있으니, 설치 후 안 되면 그 두 파일의 KMP_DUPLICATE_LIB_OK/
# OMP_NUM_THREADS 관련 주석부터 참고할 것.
#
# openpyxl은 코드에서 직접 import하진 않지만 agent/mapping/golden_set.py가
# pandas.read_excel()로 .xlsx 골든셋을 읽을 때 pandas가 내부적으로 필요로 함.

pandas>=3.0.5
openpyxl>=3.1.5
python-dotenv>=1.2.2
requests>=2.34.2
sentence-transformers>=5.6.1
# db/fetch_article_text.py(프론트 "기사 팩트체크 뷰어"용 원문 재수집)가 씀. 없어도
# db/export_json.py는 안 죽고 data_set.csv 스크랩 본문으로 폴백함(export_article_texts
# 참고).
trafilatura>=2.2.0
# agent/kosis/build_vdb_index.py, query_vdb.py — VDB를 Chroma에서 Supabase(pgvector)로
# 옮기면서 Postgres 연결용으로 추가 (2026-08-18).
psycopg2-binary>=2.9.9
# 형태소 분석기 — keyword_search.py의 _kiwi, calc_type_router.py의 해외국가 오탐 방지,
# batch_runner.py의 "달/월" 오탐 방지 등 여러 정확도 안전장치가 이걸 전제로 만들어졌는데
# 여태 이 파일에 빠져 있었다(2026-08-20 발견) — 이것 없이 pip install -r requirements.txt만
# 하면 각 모듈이 조용히 단순 부분 문자열 매칭 폴백으로 저하된다(에러 없이 동작은 하지만
# 정확도가 떨어짐). 각 모듈의 try/except 폴백 자체는 kiwipiepy가 정말 없는 환경(예: 아직
# 지원 안 되는 파이썬 버전)에서 완전히 죽지 않기 위한 안전장치로 그대로 남겨두고, 정상
# 환경에서는 항상 설치되게 여기 추가한다.
kiwipiepy>=0.20.0
